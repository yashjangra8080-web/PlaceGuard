export function canonicalEvent(event) { return JSON.stringify({ actor_id: event.actor_id ?? null, actor_role: event.actor_role ?? null, action_type: event.action_type, entity_type: event.entity_type, entity_id: event.entity_id ?? null, reason: event.reason ?? null, metadata: event.metadata ?? {}, status: event.status }) }
export async function sha256(input) { const data = new TextEncoder().encode(input); const digest = await crypto.subtle.digest('SHA-256', data); return [...new Uint8Array(digest)].map((byte) => byte.toString(16).padStart(2, '0')).join('') }
export async function verifyAuditEvents(events, hash = sha256) { let previousHash = 'GENESIS'; for (let index = 0; index < events.length; index += 1) { const event = events[index]; const expected = await hash(canonicalEvent(event) + previousHash); if (event.previous_hash !== previousHash || event.event_hash !== expected) return { valid: false, checked: index + 1, brokenAt: event.sequence_number }; previousHash = event.event_hash } return { valid: true, checked: events.length, brokenAt: null } }
